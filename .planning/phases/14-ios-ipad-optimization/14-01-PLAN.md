---
phase: 14-ios-ipad-optimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/drills/TagInput.tsx
  - frontend/src/components/MediaUpload.tsx
  - frontend/src/pages/LoginPage.tsx
  - frontend/src/pages/SignupPage.tsx
autonomous: true

must_haves:
  truths:
    - "All interactive elements have 44px minimum touch targets"
    - "Tag remove buttons are easily tappable on iPad"
    - "MediaUpload action buttons are easily tappable on iPad"
    - "Auth page navigation links are easily tappable on iPad"
  artifacts:
    - path: "frontend/src/components/drills/TagInput.tsx"
      provides: "Tag remove buttons with 44px touch targets"
      contains: "min-h-11"
    - path: "frontend/src/components/MediaUpload.tsx"
      provides: "Action buttons with 44px touch targets"
      contains: "min-h-11"
    - path: "frontend/src/pages/LoginPage.tsx"
      provides: "Signup link with 44px touch target"
      contains: "min-h-11"
    - path: "frontend/src/pages/SignupPage.tsx"
      provides: "Login link with 44px touch target"
      contains: "min-h-11"
  key_links:
    - from: "TagInput.tsx"
      to: "Tailwind min-h-11"
      via: "className"
      pattern: "min-h-11"
---

<objective>
Fix touch target sizing on components identified in Phase 14 audit as below 44px minimum.

Purpose: Ensure all interactive elements meet iOS accessibility requirements (44px touch targets) for field use on iPads.
Output: Updated components with proper touch target sizing using Tailwind min-h-11 class.
</objective>

<execution_context>
@/Users/seijimatsuda/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seijimatsuda/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-ios-ipad-optimization/14-RESEARCH.md

# Components to fix
@frontend/src/components/drills/TagInput.tsx
@frontend/src/components/MediaUpload.tsx
@frontend/src/pages/LoginPage.tsx
@frontend/src/pages/SignupPage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix TagInput tag remove buttons</name>
  <files>frontend/src/components/drills/TagInput.tsx</files>
  <action>
Fix the tag remove button touch target from 20px to 44px minimum.

Current (line 103-108):
```tsx
<button
  type="button"
  onClick={() => removeTag(index)}
  className="flex items-center justify-center rounded-sm hover:bg-blue-200 focus:outline-none focus:ring-2 focus:ring-blue-500"
  style={{ minWidth: "20px", minHeight: "20px" }}
  aria-label={`Remove ${tag}`}
>
```

Change to:
```tsx
<button
  type="button"
  onClick={() => removeTag(index)}
  className="min-h-11 min-w-11 flex items-center justify-center rounded-sm hover:bg-blue-200 focus:outline-none focus:ring-2 focus:ring-blue-500 -mr-2"
  aria-label={`Remove ${tag}`}
>
```

Key changes:
- Remove inline style (minWidth/minHeight)
- Add Tailwind classes: min-h-11 min-w-11 (44px each)
- Add -mr-2 negative margin to prevent excessive spacing between tags

Keep the icon size at h-4 w-4 (16px) - the touch area extends beyond the visible icon which is valid.
  </action>
  <verify>
Run TypeScript check: `cd frontend && npx tsc --noEmit`
Visually confirm button renders without breaking tag layout (dev server).
  </verify>
  <done>Tag remove buttons have min-h-11 min-w-11 classes and render correctly without layout issues.</done>
</task>

<task type="auto">
  <name>Task 2: Fix MediaUpload action buttons</name>
  <files>frontend/src/components/MediaUpload.tsx</files>
  <action>
Fix three inline text buttons that lack proper touch targets: Cancel, Delete, Try again.

1. Cancel button (line 156-162) - during upload progress:
```tsx
// Current:
<button
  onClick={handleCancel}
  className="text-sm text-red-600 hover:text-red-800 font-medium"
  type="button"
>
  Cancel
</button>

// Change to:
<button
  onClick={handleCancel}
  className="min-h-11 px-3 text-sm text-red-600 hover:text-red-800 font-medium"
  type="button"
>
  Cancel
</button>
```

2. Try again button (line 181-187) - after upload error:
```tsx
// Current:
<button
  onClick={reset}
  className="mt-2 text-sm text-red-600 hover:text-red-800 font-medium"
  type="button"
>
  Try again
</button>

// Change to:
<button
  onClick={reset}
  className="mt-2 min-h-11 px-3 text-sm text-red-600 hover:text-red-800 font-medium"
  type="button"
>
  Try again
</button>
```

3. Delete button (line 225-231) - after upload complete:
```tsx
// Current:
<button
  onClick={handleDelete}
  disabled={isDeleting}
  className="text-sm text-red-600 hover:text-red-800 font-medium disabled:opacity-50"
  type="button"
>
  {isDeleting ? 'Deleting...' : 'Delete'}
</button>

// Change to:
<button
  onClick={handleDelete}
  disabled={isDeleting}
  className="min-h-11 px-3 text-sm text-red-600 hover:text-red-800 font-medium disabled:opacity-50"
  type="button"
>
  {isDeleting ? 'Deleting...' : 'Delete'}
</button>
```

Key pattern: Add `min-h-11 px-3` to each button - 44px height with horizontal padding for comfortable tapping.
  </action>
  <verify>
Run TypeScript check: `cd frontend && npx tsc --noEmit`
Visually confirm buttons render correctly in upload progress, error, and complete states.
  </verify>
  <done>All three MediaUpload action buttons have min-h-11 class for 44px touch targets.</done>
</task>

<task type="auto">
  <name>Task 3: Fix auth page navigation links</name>
  <files>
frontend/src/pages/LoginPage.tsx
frontend/src/pages/SignupPage.tsx
  </files>
  <action>
Fix the navigation links at the bottom of auth pages that lack touch target sizing.

1. LoginPage.tsx (line 75-80) - "Sign up" link:
```tsx
// Current:
<p className="mt-6 text-center text-sm text-gray-600">
  Don't have an account?{' '}
  <Link to="/signup" className="text-blue-600 hover:underline">
    Sign up
  </Link>
</p>

// Change to:
<p className="mt-6 text-center text-sm text-gray-600">
  Don't have an account?{' '}
  <Link to="/signup" className="inline-flex items-center min-h-11 text-blue-600 hover:underline">
    Sign up
  </Link>
</p>
```

2. SignupPage.tsx (line 110-115) - "Log in" link:
```tsx
// Current:
<p className="mt-6 text-center text-sm text-gray-600">
  Already have an account?{' '}
  <Link to="/login" className="text-blue-600 hover:underline">
    Log in
  </Link>
</p>

// Change to:
<p className="mt-6 text-center text-sm text-gray-600">
  Already have an account?{' '}
  <Link to="/login" className="inline-flex items-center min-h-11 text-blue-600 hover:underline">
    Log in
  </Link>
</p>
```

Key pattern: Add `inline-flex items-center min-h-11` to Link components - maintains inline text flow while ensuring 44px touch area.
  </action>
  <verify>
Run TypeScript check: `cd frontend && npx tsc --noEmit`
Visually confirm links display correctly and maintain visual alignment with surrounding text.
  </verify>
  <done>Auth page navigation links have min-h-11 for 44px touch targets while maintaining inline text appearance.</done>
</task>

</tasks>

<verification>
After all tasks:
1. `cd frontend && npx tsc --noEmit` - no TypeScript errors
2. Visual check: TagInput tag remove buttons, MediaUpload actions, auth page links all render correctly
3. Browser DevTools: Verify computed min-height is 44px (2.75rem) on all fixed elements
</verification>

<success_criteria>
- [ ] TagInput tag remove buttons have min-h-11 min-w-11 classes
- [ ] MediaUpload Cancel/Delete/Try again buttons have min-h-11 class
- [ ] LoginPage signup link has min-h-11 class
- [ ] SignupPage login link has min-h-11 class
- [ ] All changes pass TypeScript compilation
- [ ] No visual layout regressions
</success_criteria>

<output>
After completion, create `.planning/phases/14-ios-ipad-optimization/14-01-SUMMARY.md`
</output>
