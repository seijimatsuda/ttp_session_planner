---
phase: 12-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/dashboard/index.ts
  - frontend/src/components/dashboard/QuickActions.tsx
  - frontend/src/components/dashboard/DashboardEmptyState.tsx
  - frontend/src/components/dashboard/RecentDrills.tsx
  - frontend/src/components/dashboard/RecentSessions.tsx
autonomous: true

must_haves:
  truths:
    - "QuickActions component renders Add Drill and New Session buttons"
    - "DashboardEmptyState shows welcome message with CTAs for new users"
    - "RecentDrills displays up to 4 most recent drills with loading/empty states"
    - "RecentSessions displays up to 4 most recent sessions with loading/empty states"
  artifacts:
    - path: "frontend/src/components/dashboard/QuickActions.tsx"
      provides: "Quick action cards with navigation links"
      exports: ["QuickActions"]
    - path: "frontend/src/components/dashboard/DashboardEmptyState.tsx"
      provides: "Empty state component for new users"
      exports: ["DashboardEmptyState"]
    - path: "frontend/src/components/dashboard/RecentDrills.tsx"
      provides: "Recent drills section with loading/empty/data states"
      exports: ["RecentDrills"]
    - path: "frontend/src/components/dashboard/RecentSessions.tsx"
      provides: "Recent sessions section with loading/empty/data states"
      exports: ["RecentSessions"]
    - path: "frontend/src/components/dashboard/index.ts"
      provides: "Barrel exports for dashboard components"
      exports: ["QuickActions", "DashboardEmptyState", "RecentDrills", "RecentSessions"]
  key_links:
    - from: "frontend/src/components/dashboard/QuickActions.tsx"
      to: "/drills/new, /sessions/new"
      via: "react-router-dom Link"
      pattern: "Link to="
    - from: "frontend/src/components/dashboard/RecentDrills.tsx"
      to: "useDrills hook"
      via: "React Query data fetching"
      pattern: "useDrills"
    - from: "frontend/src/components/dashboard/RecentSessions.tsx"
      to: "useSessions hook"
      via: "React Query data fetching"
      pattern: "useSessions"
---

<objective>
Create dashboard-specific components following existing codebase patterns.

Purpose: Build the foundational components needed for the dashboard landing page - quick action cards, empty state for new users, and recent items sections for drills and sessions.

Output: Four reusable dashboard components with barrel export ready for composition in DashboardPage.
</objective>

<execution_context>
@/Users/seijimatsuda/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seijimatsuda/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-dashboard/12-RESEARCH.md

# Key patterns to follow
@frontend/src/components/drills/DrillCard.tsx
@frontend/src/components/drills/DrillEmptyState.tsx
@frontend/src/hooks/useDrills.ts
@frontend/src/hooks/useSessions.ts
@frontend/src/components/ui/Button.tsx
@frontend/src/components/ui/Skeleton.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create QuickActions and DashboardEmptyState components</name>
  <files>
    frontend/src/components/dashboard/QuickActions.tsx
    frontend/src/components/dashboard/DashboardEmptyState.tsx
  </files>
  <action>
Create the dashboard directory and two components:

**QuickActions.tsx:**
- Two quick action cards in a responsive grid (1 col mobile, 2 cols sm+)
- "Add Drill" card links to `/drills/new` with video camera icon
- "New Session" card links to `/sessions/new` with calendar/grid icon
- Use `Link` from react-router-dom (NOT anchor tags)
- Each card: `min-h-11` for 44px touch targets, hover state with shadow-md
- Card structure: icon in colored bg circle, title, description
- Follow QuickActionCard pattern from research

**DashboardEmptyState.tsx:**
- Follows DrillEmptyState pattern exactly
- Welcome message for new users: "Welcome to Session Planner!"
- Description: guidance to get started
- Two CTA buttons: "Create a drill" (primary), "Plan a session" (secondary)
- Uses `Button` from `@/components/ui`
- Uses `useNavigate` for button onClick handlers
- Centered layout with icon, heading, description, buttons
  </action>
  <verify>
Run `npx tsc --noEmit` - no TypeScript errors.
Both files export their components correctly.
  </verify>
  <done>
QuickActions renders two clickable cards with proper touch targets and router navigation.
DashboardEmptyState renders welcome message with two CTAs for new users.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create RecentDrills and RecentSessions components with barrel export</name>
  <files>
    frontend/src/components/dashboard/RecentDrills.tsx
    frontend/src/components/dashboard/RecentSessions.tsx
    frontend/src/components/dashboard/index.ts
  </files>
  <action>
Create two data-fetching components following the loading/empty/data state pattern:

**RecentDrills.tsx:**
- Props: `userId: string`
- Uses `useDrills(userId)` hook to fetch all drills
- Three states:
  - Loading: Show 4 skeleton cards matching DrillCard dimensions (aspect-video + p-4 content)
  - Empty: Show "No drills yet" with Link to create first
  - Data: Show first 4 drills using existing `DrillCard` component
- Use `RECENT_ITEMS_LIMIT = 4` constant
- Grid: `grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-4`
- Import `DrillCard` from `@/components/drills/DrillCard`
- Import `Skeleton` from `@/components/ui`

**RecentSessions.tsx:**
- Props: `userId: string`
- Uses `useSessions(userId)` hook to fetch all sessions
- Same three-state pattern as RecentDrills
- For sessions (no SessionCard exists), create inline card showing:
  - Session name as heading
  - Created date formatted as "Jan 15, 2026"
  - Link to `/sessions/${session.id}` (or placeholder if route doesn't exist yet)
- Same grid layout and skeleton pattern
- Import Session type from `@/lib/database.types`

**index.ts (barrel export):**
```typescript
export { QuickActions } from './QuickActions'
export { DashboardEmptyState } from './DashboardEmptyState'
export { RecentDrills } from './RecentDrills'
export { RecentSessions } from './RecentSessions'
```
  </action>
  <verify>
Run `npx tsc --noEmit` - no TypeScript errors.
Run `npm run build` - build succeeds.
All four components exported from index.ts.
  </verify>
  <done>
RecentDrills fetches and displays up to 4 drills with loading/empty/data states.
RecentSessions fetches and displays up to 4 sessions with loading/empty/data states.
All dashboard components exported via barrel file.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no errors
2. `npm run build` succeeds
3. All four components are exported from `frontend/src/components/dashboard/index.ts`
4. Components use proper imports: `@/` path alias, react-router-dom Link, ui components
</verification>

<success_criteria>
- QuickActions component exists with two cards (Add Drill, New Session)
- DashboardEmptyState component exists with welcome message and CTAs
- RecentDrills component exists with loading/empty/data states using useDrills hook
- RecentSessions component exists with loading/empty/data states using useSessions hook
- All components follow 44px touch target convention
- All components use existing hooks and patterns (no hand-rolled data fetching)
- Barrel export makes all components available via single import
</success_criteria>

<output>
After completion, create `.planning/phases/12-dashboard/12-01-SUMMARY.md`
</output>
